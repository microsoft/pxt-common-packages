namespace pxsim.keyboard {
    export function type(s : string) {
        
    }

    export function mediaKey(key: number, event: number): void {

    }

    export function functionKey(key: number, event: number): void {

    }
}
# Control

Program controls and events.

```cards
control.millis()
control.seconds()
control.reset()
control.deviceSerialNumber()
```

## Advanced #advanced

```cards
control.raiseEvent(0, 0)
control.deviceDalVersion()
```

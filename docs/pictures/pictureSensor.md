# docs/pictures/pictureSensor

For making sensors listening to the movement and capturing of your actions like a camera.
``` ts 
   const getPictureInstance = new pictures.TakeRealPicture(5);
   getPictureInstance.setPosition(screen.width >> 1, screen.height >> 1);
```
